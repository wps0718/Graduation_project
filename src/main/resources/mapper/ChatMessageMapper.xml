<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingyuan.secondhand.mapper.ChatMessageMapper">

    <select id="selectMessagesBySessionKey" resultType="com.qingyuan.secondhand.entity.ChatMessage">
        SELECT *
        FROM chat_message
        WHERE session_key = #{sessionKey}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectMessageCount" resultType="int">
        SELECT COUNT(*)
        FROM chat_message
        WHERE session_key = #{sessionKey}
    </select>

    <update id="markAsRead">
        UPDATE chat_message
        SET is_read = 1
        WHERE receiver_id = #{receiverId}
          AND session_key = #{sessionKey}
          AND is_read = 0
    </update>

</mapper>
